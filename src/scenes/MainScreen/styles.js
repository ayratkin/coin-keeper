import { StyleSheet } from 'react-native';
import { colors } from '@styles/variables';

const Styles = StyleSheet.create({
  mainScreenWrapper: {
    flex: 1,
    width: '100%',
    height: '100%',
    backgroundColor: colors.lightCoffe,
  },
});

export default Styles;
